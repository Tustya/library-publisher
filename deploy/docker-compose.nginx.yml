# Для домена progs-nik.ru frontend уже слушает 127.0.0.1:8080 (docker-compose.yml).
# Этот файл не меняет порты; оставлен для совместимости со старыми инструкциями.
# Запуск: docker compose up -d

services:
  frontend: {}
